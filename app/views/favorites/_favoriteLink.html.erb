<% if rant.user_id == current_user.id %>
    <%= link_to "Delete", rant_path(rant), method: :delete %>
<% else %>
    <% if Favorite.where(user_id: current_user.id, rant_id: rant.id) == [] ? true : false %>
        <%= button_tag "Favorite", class: 'favoriteButton', data: {userID: userID, rantID: rantID, favCount: Favorite.where(rant_id: rantID).length} %>
    <% else %>
        <%= button_tag "#{Favorite.where(rant_id: rantID).length} -Unfavorite", class: 'favoriteButton', data: {userID: userID, rantID: rantID, favCount: Favorite.where(rant_id: rantID).length} %>
    <% end %>
<% end %>
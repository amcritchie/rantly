
<%= render 'sessionUserPanel/userPanel' %>

<article class="followingPanel">
  <h1>Following</h1>
  <hr width="90%">
  <% @followings.each do |following| %>
      <article>
        <div>
          <h4><%= User.find(following.user_being_followed_id).full_name %></h4>
          <%= image_tag User.find(following.user_being_followed_id).image_url %>

          <p><%= render partial: 'followings/followLink', locals: {following_id: following.user_being_followed_id} %></p>

        </div>
      </article>
  <% end %>
</article>